{
  "version": 2,
  "buildCommand": "npm install --include=dev && npm run build:frontend",
  "outputDirectory": "frontend/dist",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
